🛠️ Простий Клієнт-Сервер Чат (TCP Sockets)
Ці два скрипти демонструють базову спробу реалізації чат-системи, використовуючи низькорівневі TCP сокети в Python. Вони призначені для ілюстрації основних кроків: створення сокета, прив'язка адреси, прослуховування, прийом з'єднань та надсилання/отримання даних.

📜 Файли Проєкту
Серверна Частина (Server.py): Скрипт, що приймає з'єднання від клієнтів.

Клієнтська Частина (Client.py): Скрипт, який підключається до сервера та отримує дані.

⚙️ Принцип Роботи (Сервер)
Сервер створює сокет і прив'язує його до localhost:12345.

Він встановлює неблокуючий режим (setblocking(False)) для постійного перевіряння нових клієнтів (accept).

Сервер зберігає список підключених клієнтів (clients).

У нескінченному циклі він намагається:

Прийняти нове з'єднання та отримати ім'я клієнта.

Кожному підключеному клієнту розсилається (хоча тут некоректна логіка, див. "Проблеми") повідомлення, що містить його ж ім'я: 'Онлайн: {ім'я_клієнта}'.

При виникненні помилки надсилання (що зазвичай означає відключення клієнта), клієнт видаляється зі списку.

👤 Принцип Роботи (Клієнт)
Клієнт створює сокет.

Користувач вводить своє ім'я.

Клієнт некоректно прив'язується до адреси localhost:12345 (див. "Проблеми").

Клієнт відправляє своє ім'я.

Він починає нескінченний цикл отримання та друку повідомлень від сервера.

⚠️ Критичні Проблеми та Потреба у Виправленні
Ці скрипти містять критичні помилки у мережевій логіці, які не дозволять їм працювати як задумано.

1. Помилка у Клієнті: bind замість connect
Помилка: У клієнтській частині викликається метод client_socket.bind(("localhost", 12345)).

Чому це неправильно: Метод bind використовується для прив'язки сокета до локальної адреси та порту, що є першим кроком для сервера, який слухає вхідні з'єднання. Клієнт же повинен використовувати метод client_socket.connect(("localhost", 12345)), щоб ініціювати з'єднання з адресою, де працює сервер.

2. Помилка у Сервері: Відсутність Логіки recv
Проблема: Сервер не має механізму для отримання даних від клієнтів (крім першого отримання імені).

Наслідок: Сервер не може приймати жодних повідомлень, надісланих клієнтами, і, відповідно, не може пересилати їх іншим. Сервер лише безглуздо розсилає кожному клієнту його ж власне ім'я. Для коректної роботи чату потрібен внутрішній цикл, який перевіряє, чи не надіслав клієнт нове повідомлення.

3. Блокування Клієнта
Проблема: Клієнтська частина викликає client_socket.recv(1024).decode() у блокуючому режимі.

Наслідок: Якщо сервер не відправить клієнту повідомлення, програма-клієнт зависне на цьому рядку, очікуючи даних, і не зможе виконувати інші дії, наприклад, відправляти повідомлення користувача. Це вимагає використання неблокуючого режиму або багатопотоковості.

Що потрібно для коректної роботи:

Щоб ці скрипти стали повноцінною чат-системою, необхідно:

У клієнті замінити bind на connect.

У клієнті додати потік для відправлення повідомлень, введених користувачем.

У сервері додати логіку отримання повідомлень від усіх клієнтів (потрібна функція select або багатопотоковість) і лише потім їхню розсилку.
